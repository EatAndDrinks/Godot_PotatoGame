---
title: 人工智能导论 第5章 非经典推理 完整习题解答
author: 豆包AI
date: 2026年6月5日
documentclass: article
geometry: a4paper, margin=1in
linestretch: 1.2
toc: true
toc-depth: 2
numbersections: true
---

# 核心知识点回顾

## 1.1 缺省理论
缺省理论是Reiter于1980年提出的一种处理不完全知识的非单调推理理论，由二元组表示：
$$\boldsymbol{\Delta=(D,W)}$$
- $\boldsymbol{W}$：**硬性事实集合**，由一阶逻辑公式组成，表示必然成立、不可推翻的知识。
- $\boldsymbol{D}$：**缺省规则集合**，由可废止的推理规则组成，表示在没有反例时默认成立的知识。

缺省规则的通用表示形式为：
$$\frac{\alpha(x):\beta_1(x),\beta_2(x),\dots,\beta_n(x)}{\gamma(x)}$$
其中：
- $\boldsymbol{\alpha(x)}$：**先决条件**，触发该规则必须满足的已知事实。
- $\boldsymbol{\beta_i(x)}$：**缺省理由**，一致性条件：若无法证明$\neg\beta_i(x)$成立，则认为$\beta_i(x)$与当前知识库相容。
- $\boldsymbol{\gamma(x)}$：**结论**，当先决条件满足且所有缺省理由都相容时，可推出的缺省结论。

## 1.2 主观贝叶斯推理
主观贝叶斯方法通过**充分性度量LS**和**必要性度量LN**来描述证据与结论之间的关系。
- 先验几率：$O(H) = \frac{P(H)}{1-P(H)}$
- 证据$E$出现时的后验几率：$O(H|E) = LS \cdot O(H)$
- 证据$E$不出现时的后验几率：$O(H|\neg E) = LN \cdot O(H)$
- 后验概率：$P(H|E) = \frac{O(H|E)}{1+O(H|E)}$
- 多个独立证据组合：$O(H|E_1,E_2,\dots,E_n) = \left(\prod_{i=1}^n LS_i\right) \cdot O(H)$（当$LN_i=1$时，证据不出现不影响结论）

## 1.3 证据理论（D-S理论）
- **基本概率分配（BPA）**：函数$m:2^U \to [0,1]$，满足$m(\emptyset)=0$且$\sum_{A \subseteq U} m(A)=1$。
- **信任函数（Bel）**：$Bel(A) = \sum_{B \subseteq A} m(B)$，表示对$A$为真的总信任度。
- **似然函数（Pl）**：$Pl(A) = 1 - Bel(\neg A) = \sum_{B \cap A \neq \emptyset} m(B)$，表示对$A$为假的不信任程度。
- **类概率函数**：$f(A) = Bel(A) + \frac{|A|}{|U|} \cdot (Pl(A) - Bel(A))$，其中$|A|$是集合$A$的元素个数，$|U|$是论域大小。

\newpage

# 5.2 缺省规则的表示与形式

## 问题
在缺省理论中，缺省规则是如何表示的？有哪几种表示形式？

## 解答

### 1. 缺省规则的标准表示
如上述1.1节所述，缺省规则采用分数形式表示，直观表达了“前提成立且理由相容则推出结论”的推理逻辑。

**直观语义**：如果已知$\alpha(x)$为真，并且对于每个$\beta_i(x)$，都不能证明它是假的（即$\beta_i(x)$与当前所有已知事实不矛盾），那么我们就可以默认推出$\gamma(x)$为真。

### 2. 三种主要表示形式
根据缺省理由与结论的关系，缺省规则可分为以下三类：

| 类型 | 语法形式 | 核心特征 | 典型例子 |
|------|----------|----------|----------|
| **正规缺省** | $\displaystyle \frac{\alpha:\gamma}{\gamma}$ | 缺省理由与结论完全相同 | $\displaystyle \frac{Bird(x):Fly(x)}{Fly(x)}$<br>（鸟会飞，除非证明不能飞） |
| **半正规缺省** | $\displaystyle \frac{\alpha:\gamma\land\delta}{\gamma}$ | 缺省理由是结论与一个附加约束的合取 | $\displaystyle \frac{Bird(x):Fly(x)\land\neg Penguin(x)}{Fly(x)}$<br>（鸟会飞，除非是企鹅） |
| **一般缺省** | $\displaystyle \frac{\alpha:\beta_1,\dots,\beta_n}{\gamma}$ | 缺省理由与结论无强制关联，通用性最强 | $\displaystyle \frac{HasWings(x):Bird(x),Fly(x)}{CanFly(x)}$ |

**补充特例**：无前提缺省规则 $\displaystyle \frac{:\beta}{\gamma}$，表示无需任何先决条件，只要$\beta$与知识库一致，就可以推出$\gamma$。

\newpage

# 5.3 缺省理论建模

## 问题
用缺省理论表示下面的句子，并给出$D$和$W$集合。
(a) 有些软体动物(molluscs)是有壳动物(shell-bearers)。
(b) 头足类动物(cephalopods)是软体动物。
(c) 头足类动物不是有壳动物。

## 解答

### 步骤1：定义谓词符号
首先对题目中的概念进行一阶逻辑符号化：
- $M(x)$：$x$是软体动物
- $S(x)$：$x$是有壳动物
- $C(x)$：$x$是头足类动物

### 步骤2：逐条分析句子性质
- **句子(b)：头足类动物是软体动物**
  这是一个**全称硬性事实**，所有头足类动物必然属于软体动物，不存在例外。因此应放入**硬性事实集合$W$**。
  一阶逻辑表示：$\forall x \big(C(x)\rightarrow M(x)\big)$

- **句子(c)：头足类动物不是有壳动物**
  这也是一个**全称硬性事实**，是生物学上的客观定论，不存在例外。因此也应放入**硬性事实集合$W$**。
  一阶逻辑表示：$\forall x \big(C(x)\rightarrow \neg S(x)\big)$

- **句子(a)：有些软体动物是有壳动物**
  这句话的实际含义是：**大多数软体动物都有壳，但存在例外（如头足类）**。这是一种典型的缺省知识，不能用全称量词表示（否则会与句子(c)矛盾），也不能用存在量词表示（存在量词无法用于推理）。因此应放入**缺省规则集合$D$**。
  缺省规则表示：$\displaystyle d_1=\frac{M(x):S(x)}{S(x)}$
  语义：如果$x$是软体动物，且不能证明$x$没有壳，那么默认$x$是有壳动物。

### 步骤3：写出最终的$D$和$W$集合
$$
\begin{align*}
\boldsymbol{W} &= \big\{ \forall x\big(C(x)\rightarrow M(x)\big),\ \forall x\big(C(x)\rightarrow \neg S(x)\big) \big\} \\
\boldsymbol{D} &= \big\{ \frac{M(x):S(x)}{S(x)} \big\}
\end{align*}
$$

### 步骤4：推理效果验证
我们可以通过两个例子验证该缺省理论的正确性：
1. **普通软体动物（如蜗牛）**：
   已知$M(蜗牛)$，且知识库中没有$\neg S(蜗牛)$的证据。因此缺省规则$d_1$被触发，推出$S(蜗牛)$，符合常识。

2. **头足类动物（如章鱼）**：
   已知$C(章鱼)$，由$W$中的事实可推出$M(章鱼)$和$\neg S(章鱼)$。虽然满足缺省规则$d_1$的先决条件$M(章鱼)$，但缺省理由$S(章鱼)$与已知事实$\neg S(章鱼)$矛盾，因此$d_1$不会被触发，不会错误推出$S(章鱼)$。

\newpage

# 5.9 主观贝叶斯推理（一）

## 问题
设有如下规则：
$r_1: E_1 \to H \quad LS=10, \quad LN=1$
$r_2: E_2 \to H \quad LS=20, \quad LN=1$
$r_3: E_3 \to H_1 \quad LS=1, \quad LN=0.002$
已知$H$、$H_1$的先验概率$P(H)=0.03$、$P(H_1)=0.3$。
(1) 若证据$E_1$、$E_2$依次出现，按主观Bayes推理，求$H$在此条件下的概率$P(H|E_1,E_2)$。
(2) 对$r_3$求$P(H_1|E_3)$、$P(H_1|\neg E_3)$的值各是多少？

## 解答

### (1) 计算$P(H|E_1,E_2)$
**步骤1：计算$H$的先验几率**
$$O(H) = \frac{P(H)}{1-P(H)} = \frac{0.03}{1-0.03} = \frac{0.03}{0.97} \approx 0.030928$$

**步骤2：计算$E_1$出现后的后验几率**
$$O(H|E_1) = LS_1 \cdot O(H) = 10 \times 0.030928 \approx 0.30928$$

**步骤3：计算$E_1$、$E_2$同时出现后的后验几率**
$$O(H|E_1,E_2) = LS_2 \cdot O(H|E_1) = 20 \times 0.30928 \approx 6.1856$$

**步骤4：转换为后验概率**
$$P(H|E_1,E_2) = \frac{O(H|E_1,E_2)}{1+O(H|E_1,E_2)} = \frac{6.1856}{1+6.1856} \approx \boldsymbol{0.8608}$$

### (2) 计算$P(H_1|E_3)$和$P(H_1|\neg E_3)$
- **计算$P(H_1|E_3)$**：
  由于$LS_3=1$，说明证据$E_3$的出现对结论$H_1$没有影响，因此：
  $$P(H_1|E_3) = P(H_1) = \boldsymbol{0.3}$$

- **计算$P(H_1|\neg E_3)$**：
  首先计算$H_1$的先验几率：
  $$O(H_1) = \frac{P(H_1)}{1-P(H_1)} = \frac{0.3}{1-0.3} = \frac{0.3}{0.7} \approx 0.42857$$
  然后计算$E_3$不出现后的后验几率：
  $$O(H_1|\neg E_3) = LN_3 \cdot O(H_1) = 0.002 \times 0.42857 \approx 0.0008571$$
  最后转换为后验概率：
  $$P(H_1|\neg E_3) = \frac{O(H_1|\neg E_3)}{1+O(H_1|\neg E_3)} = \frac{0.0008571}{1+0.0008571} \approx \boldsymbol{0.000856}$$

\newpage

# 5.10 主观贝叶斯推理（二）

## 问题
设有如下规则：
$r_1: E_1 \to H \quad LS=20, \quad LN=1$
$r_2: E_2 \to H \quad LS=300, \quad LN=1$
已知$H$的先验概率$P(H)=0.03$，若证据$E_1$、$E_2$依次出现，按主观Bayes推理，求$H$在此条件下的概率$P(H|E_1,E_2)$。

## 解答
**步骤1：计算$H$的先验几率**
$$O(H) = \frac{0.03}{1-0.03} \approx 0.030928$$

**步骤2：计算$E_1$出现后的后验几率**
$$O(H|E_1) = LS_1 \cdot O(H) = 20 \times 0.030928 \approx 0.61856$$

**步骤3：计算$E_1$、$E_2$同时出现后的后验几率**
$$O(H|E_1,E_2) = LS_2 \cdot O(H|E_1) = 300 \times 0.61856 \approx 185.568$$

**步骤4：转换为后验概率**
$$P(H|E_1,E_2) = \frac{185.568}{1+185.568} \approx \boldsymbol{0.9946}$$

\newpage

# 5.17 证据理论类概率函数计算

## 问题
已知$f_1(E_1)=0.8$，$f_1(E_2)=0.6$，$|U|=20$，$E_1 \land E_2 \to H=\{h_1,h_2\}$，$(c_1,c_2)=(0.3,0.5)$，计算$f_1(H)$。

## 解答
**步骤1：计算合取证据的可信度**
对于合取证据$E_1 \land E_2$，其可信度取各证据可信度的最小值：
$$f(E_1 \land E_2) = \min(f_1(E_1), f_1(E_2)) = \min(0.8, 0.6) = 0.6$$

**步骤2：计算结论$H$的基本概率分配**
根据规则，将证据的可信度按比例分配给$H$的各个子集：
$$m(\{h_1\}) = c_1 \cdot f(E_1 \land E_2) = 0.3 \times 0.6 = 0.18$$
$$m(\{h_2\}) = c_2 \cdot f(E_1 \land E_2) = 0.5 \times 0.6 = 0.3$$
剩余的可信度分配给论域$U$：
$$m(U) = 1 - m(\{h_1\}) - m(\{h_2\}) = 1 - 0.18 - 0.3 = 0.52$$

**步骤3：计算信任函数$Bel(H)$和似然函数$Pl(H)$**
$$Bel(H) = m(\{h_1\}) + m(\{h_2\}) = 0.18 + 0.3 = 0.48$$
$$Pl(H) = Bel(H) + m(U) = 0.48 + 0.52 = 1.0$$

**步骤4：计算类概率函数$f_1(H)$**
已知$|H|=2$，$|U|=20$，因此：
$$f_1(H) = Bel(H) + \frac{|H|}{|U|} \cdot (Pl(H) - Bel(H))$$
$$= 0.48 + \frac{2}{20} \times (1.0 - 0.48)$$
$$= 0.48 + 0.1 \times 0.52 = \boldsymbol{0.532}$$

\newpage

# 5.18 证据理论信任与似然函数计算

## 问题
考生考试成绩的论域为$\{A,B,C,D,E\}$，小王成绩为$A$、为$B$、为$A$或$B$的基本概率分别分配为$0.2$、$0.1$、$0.3$。$Bel(\{C,D,E\})=0.2$。请给出$Bel(\{A,B\})$、$Pl(\{A,B\})$和$f(\{A,B\})$。

## 解答
**步骤1：计算信任函数$Bel(\{A,B\})$**
信任函数是所有包含于$\{A,B\}$的子集的基本概率之和：
$$Bel(\{A,B\}) = m(\{A\}) + m(\{B\}) + m(\{A,B\})$$
$$= 0.2 + 0.1 + 0.3 = \boldsymbol{0.6}$$

**步骤2：计算似然函数$Pl(\{A,B\})$**
似然函数等于1减去$\{A,B\}$补集的信任函数：
$$Pl(\{A,B\}) = 1 - Bel(\neg\{A,B\}) = 1 - Bel(\{C,D,E\})$$
$$= 1 - 0.2 = \boldsymbol{0.8}$$

**步骤3：计算类概率函数$f(\{A,B\})$**
已知$|H|=|\{A,B\}|=2$，$|U|=5$，因此：
$$f(\{A,B\}) = Bel(\{A,B\}) + \frac{|H|}{|U|} \cdot (Pl(\{A,B\}) - Bel(\{A,B\}))$$
$$= 0.6 + \frac{2}{5} \times (0.8 - 0.6)$$
$$= 0.6 + 0.4 \times 0.2 = \boldsymbol{0.68}$$

\newpage

# 关键说明与易错点

## 6.1 缺省理论
- 放入$W$的知识：**绝对正确，不可推翻**，无论出现什么新信息，这些事实都不会改变。
- 放入$D$的知识：**默认正确，可被推翻**，当出现与结论矛盾的新事实时，该缺省规则的结论会被撤销。
- 常见错误：将“有些”翻译成存在量词$\exists$，存在量词只能表示“存在至少一个”，无法用于推理。

## 6.2 主观贝叶斯推理
- 当$LS=1$时，证据出现对结论无影响；当$LN=1$时，证据不出现对结论无影响。
- 多个独立证据的组合是**几率的乘积**，不是概率的乘积，必须先转换为几率再计算。
- 最终结果必须从后验几率转换回后验概率。

## 6.3 证据理论
- 基本概率分配$m(A)$表示对集合$A$本身的信任度，不包括其子集。
- 信任函数$Bel(A) \leq$ 类概率函数$f(A) \leq$ 似然函数$Pl(A)$，这是一个恒成立的不等式。
- 类概率函数同时考虑了对集合的信任度和集合的大小，是证据理论中最常用的概率近似。

---

## 导出PDF操作步骤（推荐Typora）
1. 复制上面全部代码
2. 打开Typora，新建空白文档并粘贴
3. 点击顶部菜单栏「文件」→「导出」→「PDF」
4. 选择保存位置，点击「保存」即可